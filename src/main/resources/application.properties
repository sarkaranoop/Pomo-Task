spring.application.name=taskboard
# Render (and other PaaS) set PORT; local default 8080
server.port=${PORT:8080}


# PostgreSQL connection
spring.datasource.url=jdbc:postgresql://localhost:5432/focusflow
spring.datasource.username=postgres
spring.datasource.password=Qazwsxedc@!@#12
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
app.jwt.secret=focusflowsecretkeymustbeatleast32characterslong
app.jwt.expiration=86400000
